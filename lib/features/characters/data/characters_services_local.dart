part of '../characters.dart';

class CharactersServicesLocal implements CharactersServices {
  @override
  Future<ListResponse<Character>> getCharacters(FilterQuery filters) {
    throw UnimplementedError();
  }
}
